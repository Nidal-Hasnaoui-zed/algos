// ex 07 :
// Ecrire un algorithme qui demande ä l'utilisateur de saisir 2 entiers A et B,
// qui échange le contenu des variables A et B puis qui affiche A et B.
ALGORITHM change 
VAR 
    A , B : INTEGER ;
BEGIN 
    Write("Donner A"); 
    Read(A); 
    Write("Donner B"); 
    Read(B);
    C <-- A ; 
    A <-- B ; 
    B <-- C ;  
    Write("La valeur de A est ," +A) ; 
    Write("La valeur de B est " + B) ; 
END
